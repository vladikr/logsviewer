import * as React from 'react';
import { PageSection, Title } from '@patternfly/react-core';

const ImportDatabase: React.FunctionComponent = () => (
  <PageSection>
    <Title headingLevel="h1" size="lg">
      Import Database Page Title
    </Title>
  </PageSection>
);

export { ImportDatabase };
